[package]
name = "gf"
version = "0.1.0"
edition = "2021"
description = "Opinionated trunk-based development CLI built for agentic, monorepo workflows"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"
