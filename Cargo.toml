[package]
name = "flow"
version = "0.1.0"
edition = "2021"
description = "A minimal, opinionated CLI for trunk-based development with worktree escape hatches"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"
